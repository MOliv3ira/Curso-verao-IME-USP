#include <stdio.h>
#include <stdlib.h>
int main() {

  int n;
  scanf("%d", &n);

  j = n * 2 - 1;

        for(int i = 0; i < n*2; i++){
           if(i % 2 == 0){
            printf("%d", i);

            }
            printf("%d", j, j-2);
    }

    system("pause");
return 0;
}
